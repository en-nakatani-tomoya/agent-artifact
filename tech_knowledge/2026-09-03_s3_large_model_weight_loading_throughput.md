# s3 large model weight loading throughput

## 概要
S3 上の巨大な単一ファイル（26.7 GiB の model.safetensors）を GPU へロードする際の高速化手法を実測比較した記録。ダウンロード並列化とマウント方式の違い、および逐次ベンチでストレージを選ぶと実ワークロードで逆の結論になる罠。

## 詳細
## 前提となった構成

GPU インスタンス（L40S 1 枚）上の vLLM で、26.7 GiB の単一 `model.safetensors`（FP8 量子化済み MoE モデル）を S3 から読み込むバッチ。ロード時間がジョブ全体の 72%（1,551 秒中 1,119 秒）を占めていた。

## 1. 遅さの正体は「S3 が遅い」ではなく「ストリームが 1 本」

S3 は 1 本の TCP コネクションあたりのスループットに実質的な上限があり、アグリゲート帯域はコネクション数で稼ぐ設計になっている。

`pyarrow.fs.copy_files` の並列度は **ファイル単位**。したがって巨大な単一ファイルは何をしても 1 ストリームに落ちる。実測 25.6 MB/s は S3 の速度ではなく 1 コネクションの速度だった。

boto3 の `TransferConfig`（マルチパート並列ダウンロード）に差し替えると 208 秒 = 138 MB/s で 5.4 倍になる。同じ 1 ファイルをレンジ GET で分割して複数コネクションに散らすため。`aws s3 cp` が速いと言われるのも中身はこの s3transfer エンジンそのもので、速さの出所はコマンドではなく並列レンジ GET にある。

## 2. マウントが効くのは「往復が消える」から

ダウンロード方式とマウント方式の本質的な差は並列度ではなく、バイトが何回運ばれるか。

| 方式 | バイトの経路 | 26.7 GiB を運ぶ回数 |
| --- | --- | --- |
| ダウンロード（copy_files / boto3 / aws s3 cp） | S3 → EBS へ書く → EBS から読む → GPU | 2 回・直列 |
| マウント（S3 Files / Mountpoint） | S3 → ページキャッシュ → GPU | 1 回 |

- ダウンロード完了を待ってからロードを開始する、という直列の待ちが構造的に消える
- EBS を経由しないので gp3 のスループット上限を踏まない（既定 125 MiB/s では 26.7 GiB の書き込みだけで 219 秒の床ができる。ここを明示的に 250 MiB/s にしてあるかどうかで過去の計測結果が大きくブレていた）
- アプリからはローカルパスを開いているだけに見え、並列レンジ GET はマウント層が引き受ける。アプリ側のダウンロード処理コードを丸ごと削除できる

## 3. 最大の学び: 逐次ベンチで選ぶと逆の結論になる

同一インスタンス・同一ファイルで Amazon S3 Files（NFS v4.2 / EFS ベース）と Mountpoint for Amazon S3（FUSE）を比較したところ、マイクロベンチと実ワークロードで**順位が逆転した**。

| | 単一スレッド逐次読み（dd / mmap） | vLLM の実ロード |
| --- | --- | --- |
| Mountpoint | **971 MB/s** | 185 MB/s（7 割減） |
| S3 Files | 277 MB/s | **424 MB/s**（1.5 倍増） |

理由はアクセスパターン。vLLM の safetensors ロードはヘッダを読んだあと**テンサ単位でシークして拾う**ので、ファイルを頭から舐める動きにはならない。実質ランダムアクセス＋並行アクセスになる。

- **Mountpoint** はオブジェクト単位の逐次プリフェッチに最適化されている。先読みした範囲と違うオフセットに飛ばれるたびに先読みが無駄になりやり直しになるため、逐次では最速なのに実ロードでは崩れる。この兆候はマイクロベンチ段階でも並列読み 83.1 秒 対 逐次 29.5 秒という形で見えていた
- **S3 Files** は NFS なのでクライアントが独立した RPC を並行に飛ばせる。順序が飛んでも各リクエストが独立しているため崩れず、並行度が上がった分だけ**逐次より速くなった**

Mountpoint 側の劣化理由はプリフェッチ設計と上記の並列読み劣化からの推論で、内部計装は取っていない。順位そのものは実測。

**教訓: ストレージ層の選定を `dd` や `mmap` の逐次ベンチだけで判断してはいけない。実際のアクセスパターンで測らないと逆の結論が出る。** 今回、逐次ベンチだけを見ていれば Mountpoint を選び、本番で 2.3 倍遅い構成を採用していた。

## 4. 結果と次のボトルネック

| 方式 | 重み取得 | 現状比 |
| --- | --- | --- |
| pyarrow.fs.copy_files | 1,119 秒 | — |
| boto3 マルチパート → EBS → 読む | 208 秒 | 5.4 倍 |
| Mountpoint + vLLM | 154 秒 | 7.3 倍 |
| **S3 Files + vLLM** | **67.5 秒** | **16.6 倍** |

I/O を潰すとボトルネックが移る。67.5 秒になった時点での内訳は 重み取得 70.5 秒 / エンジン初期化 121.3 秒（うち **torch.compile 65.2 秒**）。`docker run --rm` が `/root/.cache/vllm/torch_compile_cache` を毎回破棄しているため、コンパイル 65 秒を毎 run 支払っていた。ここから先は I/O をさらに倍速にしても取り分 34 秒に対し、コンパイルキャッシュの永続化が 65 秒。**投資対効果が逆転する**。

## 5. 採用時の前提条件の差

| | S3 Files | Mountpoint |
| --- | --- | --- |
| 実体 | NFS v4.2/4.1（EFS ベース） | FUSE（静的バイナリ） |
| バケット側の要件 | **バージョニング有効 + SSE-S3/SSE-KMS が必須** | なし |
| ネットワーク | VPC 内にマウントターゲット + TCP 2049 | 不要 |
| IAM | ファイルシステム用サービスロールが別途必要 | インスタンスロールのみ |
| クライアント要件 | amazon-efs-utils >= 3.0.0 / AWS CLI >= 2.34 | rpm を入れるだけ |
| nconnect | **非対応** | — |
| 読み取り上限 | クライアントあたり 3 GiB/s。1 MiB 以上の読みは S3 から直接ストリームされ、ファイルシステムのデータ転送料金がかからない | — |

S3 Files の方が速いが、バケットのバージョニング必須・マウントターゲット・追加 IAM ロールと前提条件は重い。Terraform 化のコストを含めて判断する。

なお「Mountpoint は mmap 非対応」というコミュニティ報告があるが、1.24.0 では mmap は動作した（かつ逐次ベンチでは最速だった）。Docker に FUSE マウントを見せるには `--allow-other` が必要。

## コラム: 前提知識の整理

### ページキャッシュとは、どこのキャッシュか

「S3 のキャッシュ」でも「マウント層のキャッシュ」でもなく、**EC2 インスタンスのホスト RAM 上で Linux カーネルが持つファイルキャッシュ**のこと。

Linux ではファイルの読み書きは原則すべてここを通る。カーネルはファイル内容を 4 KiB のページ単位で RAM に保持し、`read()` が来たらまずここを見て、無ければ下位（ディスク、あるいはネットワーク越しの S3）へ取りに行く。取得したバイトはページキャッシュに置かれ、そこからユーザー空間へコピーされる。

vLLM は safetensors を `mmap()` で開くのでもう一段直接的になる。`mmap` はページキャッシュのページを**プロセスの仮想アドレス空間に直接貼り付ける**（コピーを 1 回省く）。未アクセスのアドレスに触るとページフォルトが起き、カーネルがその場で該当範囲を取りに行く。「S3 → ページキャッシュ → GPU」という経路はこれを指す。

GPU へは、ページキャッシュ上のバイトをピン留めホストメモリへコピーし、PCIe 経由で DMA して VRAM に入る。ここを飛ばしてストレージから直接 VRAM へ入れるのが GPUDirect Storage（今回は未使用）。

**計測上の重大な落とし穴**: インスタンスの RAM が 30 GiB、対象ファイルが 26.7 GiB とほぼ同サイズだった。2 回目以降はファイルが丸ごとページキャッシュに載り、ネットワークを一切使わない数字が出る。各計測の前に `echo 3 > /proc/sys/vm/drop_caches` を必ず入れること。忘れると全方式が「速い」という無意味な結果になる。

### NFS とは何か

Network File System。1984 年 Sun 由来、今も現役。**リモートのファイルシステムをローカルのファイルシステムに見せるプロトコル**。

アプリが `open()` / `read()` / `stat()` を呼ぶと、カーネルの **VFS**（Virtual File System = ファイルシステム抽象層）が受ける。VFS は対象が ext4 ならブロックデバイスへ、NFS マウントなら NFS クライアントモジュールへ渡す。NFS クライアントはその操作をネットワーク越しのメッセージに翻訳してサーバへ投げ、返事を待ってアプリに返す。

```
アプリ:  read(fd, buf, 1MiB)
  ↓
VFS（カーネルのファイルシステム抽象層）
  ↓
NFS クライアント → 「ファイル X のオフセット Y から 1 MiB 読め」というメッセージ
  ↓  TCP 2049 番ポート
NFS サーバ（S3 Files の場合は AWS 側のマウントターゲット）
  ↓
S3 バケット
```

決定的なのは、**アプリは自分がネットワーク越しに読んでいることを知らない**点。ただのローカルパスに見えるので、`LLM(model='/mnt/s3files')` と渡すだけで動き、アプリ側のダウンロード処理コードが丸ごと不要になる。

- **Amazon S3 Files**: AWS が S3 バケットの前に NFS v4.2/4.1 エンドポイントを立てるサービス（内部実装は EFS ベース）。「マウントターゲットが要る」「TCP 2049 を開ける」「amazon-efs-utils が要る」という前提条件は、すべて NFS そのものの要件に由来する
- **Mountpoint**: NFS ではない。**FUSE**（Filesystem in Userspace）で、VFS からの要求をユーザー空間デーモンが受け、S3 の通常の HTTPS API（レンジ付き GET）に翻訳する。ネットワーク的にはただの S3 アクセスなので、マウントターゲットも 2049 番も不要。前提条件が軽いのはこの差

### 「独立した RPC を並行に飛ばす」を OSI 7 階層で理解する

NFS は珍しく OSI モデルに素直に対応する例なので、そのまま並べられる。

| 層 | NFS（S3 Files） | Mountpoint（S3 REST） |
| --- | --- | --- |
| L7 アプリケーション | NFS 手続き（READ / GETATTR / LOOKUP …） | HTTP `GET` + `Range:` ヘッダ |
| L6 プレゼンテーション | **XDR**（RFC 4506。バイト順・構造体の符号化） | TLS / JSON・XML |
| L5 セッション | **ONC RPC**（RFC 5531）、NFSv4 のセッション | HTTP のコネクション管理 |
| L4 トランスポート | TCP 2049 | TCP 443 |
| L3 以下 | IP / ENA / … | 同左 |

「独立した RPC を並行に飛ばせる」の実体は **L5 の多重化**である。

ONC RPC の各リクエストには **XID（32 ビットの transaction identifier）**が付く。クライアントは応答を待たずに XID の異なる READ を何本も同じ TCP コネクションへ流し込み、**返ってきた応答は XID を見て元のリクエストに紐づける**。したがって応答が投げた順とバラバラに返ってきても構わない。オフセット 10 GiB の READ が先に返り、オフセット 2 GiB が後から返っても正しく処理される。

要点は、**NFS の並行性は L5 で獲得されており、L4 のコネクション本数に依存しない**こと。S3 Files が `nconnect`（L4 で TCP コネクションを増やすオプション）に非対応でも実用上そこまで困らないのは、並列度が一段上の層で確保されているため。vLLM がテンサ単位でバラバラの位置を並行要求しても「XID の違う独立した READ が複数飛ぶ」だけで、NFS にとっては素直な使われ方になる。逐次読み 277 MB/s より実ロード 424 MB/s の方が速かったのはこれが理由。

一方 Mountpoint は L7 が HTTP/1.1 で、1 本のコネクション上でリクエストを多重化できない（ヘッドオブラインブロッキング）。並行性は **L4 のコネクションプールを増やして**稼ぐモデルになる。

ただし注意点として、**Mountpoint が実ロードで遅かった直接の原因はこのコネクションモデルではなく、より上位のプリフェッチ戦略**である。層の話とプリフェッチの話は別物。前者は「NFS の並行アクセスがなぜ安いか」の説明、後者は「Mountpoint がなぜ崩れたか」の説明。

## 参考
- https://dev.classmethod.jp/articles/amazon-s3-files-ga-mount-and-compare-efs/
- https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-files.html
- https://github.com/awslabs/mountpoint-s3
- https://arxiv.org/abs/2505.23072

---
Generated: 2026-09-03
